<Page
    x:Class="object_permutator.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:object_permutator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">


    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="aUTO"/>
        </Grid.ColumnDefinitions>

        <Canvas x:Name="cv" HorizontalAlignment="Left" VerticalAlignment="Top"/>


        <ScrollViewer Grid.Column="1">
            <ListView x:Name="lv" />
        </ScrollViewer>
        <Button Content="NEXT GENERATE" HorizontalAlignment="Right" Grid.Row="1" Click="Button_Click" Grid.Column="1"/>
    </Grid>
</Page>
